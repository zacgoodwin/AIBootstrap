interface:
  display_name: "AIBootstrap"
  short_description: "Repo-specific patterns and workflows for AIBootstrap"
  default_prompt: "Use the AIBootstrap repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true